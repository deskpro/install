# vim:ft=ansible
---
- name: use all roles to install DeskPRO into a server
  hosts: 127.0.0.1
  connection: local
  sudo: yes

  roles:
    - base
    - nginx
    - php
    - elasticsearch
    - deskpro
    - firewall
    - mysql
    - config
    - install
    - security
    - cron
